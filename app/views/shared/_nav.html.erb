<nav class="navbar navbar-light bg-light px-4">
  <%= link_to t('common.project.name'), root_path, class: 'navbar-brand' %>

  <span class="navbar-text">
    <% if user_signed_in? %>
      <%= t('admin.nav.welcome') %>, <b class="user-email"><%= "#{current_user.email}" %></b>
      <%= link_to t('common.logout'), destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: t('common.actions.confirmation') } %>
    <% else %>
      <%= link_to t('common.login'), new_session_path(resource_name) %> |
      <%= link_to t('common.signup'), new_registration_path(resource_name) %>
    <% end %>
  </span>
</nav>
