<%= link_to t("common.back"), admin_tests_path %>

<h1><%= @test.title %></h1>
<p><%= "#{t('.level')} - #{test_level @test}" %></p>

<table>
  <caption><%= t('.test_questions') %></caption>

  <thead>
    <tr>
      <th><%= t('common.tables.name_column') %></th>
      <th colspan="3"><%= t('common.tables.actions_column') %></th>
    </tr>
  </thead>

  <tbody>
  <%= render partial: 'admin/questions/question', collection: @test.questions %>
  </tbody>
</table>
