<tr>
  <td><%= test.title %></td>
  <td><%= test_level(test) %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to 'Показать', admin_test_path(test) %></td>
  <td><%= link_to 'Редактировать', edit_admin_test_path(test) %></td>
  <td>
    <%= link_to 'Удалить',
        admin_test_path(test),
        data: { "turbo-method": :delete, turbo_confirm: 'Вы уверены?' } %>
  </td>
</tr>
