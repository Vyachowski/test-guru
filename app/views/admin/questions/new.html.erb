<% content_for :title do %>
  <%= t("helpers.questions.header.new", test: @answer.question.test.title) %>
<% end %>

<%= link_to t("common.back"), admin_question_path(@answer.question), class: "btn btn-secondary mb-2" %>

<h1>
  <%= t("helpers.questions.header.new", test: @answer.question.test.title) %>
</h1>

<%= render 'form', question: @answer.question, submit_text: t("helpers.submit.create", model: Question.model_name.human) %>
