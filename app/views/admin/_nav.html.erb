<nav class="navbar navbar-light bg-primary-subtle px-4">
  <div class="container p-0 mb-2">
    <%= link_to t('common.project.name'), admin_tests_path, class: 'navbar-brand' %>
    <span class="nav-admin title"><%= "#{t('.admin_panel')}" %></span>
  </div>

  <div class="container p-0">
    <p class="mb-0"><%= t('.welcome') %>, <b class="nav-admin user-email"><%= "#{current_user.email}" %></b> <%= t('common.guru') %></p>
    <%= link_to t('common.logout'), destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: t('common.actions.confirmation') } %>
  </div>
</nav>
